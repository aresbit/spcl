/= SPCL document (CCL + skills/prompt blocks)
title = SPCL Example

SKILLS:
  - modern-c-makefile
  - modern-c-dev

PROMPT:
  Rewrite this project into modern C.
  Keep parser behavior compatible with CCL core syntax.

database =
  enabled = true
  ports =
    = 8000
    = 8001
    = 8002
